<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- ===== QUINCAILLERIE (root) ===== -->
    <changeSet id="cat-quincaillerie" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Quincaillerie' AND parent_id IS NULL
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            VALUES ('Quincaillerie', 'Matériel général', NULL);
        </sql>
    </changeSet>

    <!-- ===== OUTILLAGE MANUEL ===== -->
    <changeSet id="cat-outillage-manuel" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Outillage manuel'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Outillage manuel', 'Outils manuel', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Quincaillerie' AND parent_id IS NULL) AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Outil de mesure ===== -->
    <changeSet id="cat-outil-mesure" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Outil de mesure'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Outil de mesure', 'Outil de mesure', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Outillage manuel') AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Outil de coupe ===== -->
    <changeSet id="cat-outil-coupe" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Outil de coupe'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Outil de coupe', 'Outil de coupe', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Outillage manuel') AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Outil de Serrage ===== -->
    <changeSet id="cat-outil-serrage" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Outil de Serrage'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Outil de Serrage', 'Outil de Serrage', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Outillage manuel') AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Tournevis ===== -->
    <changeSet id="cat-tournevis" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Tournevis'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Tournevis', 'Tournevis', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Outil de Serrage') AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Clés ===== -->
    <changeSet id="cat-cles" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Clés'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Clés', 'Clés', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Outil de Serrage') AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Pince ===== -->
    <changeSet id="cat-pince" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Pince'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Pince', 'Pince', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Outil de Serrage') AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Autre outils de serrage ===== -->
    <changeSet id="cat-autre-serrage" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'autre outils de serrage'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'autre outils de serrage', 'autre outils de serrage', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Outil de Serrage') AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Outils d'impact ===== -->
    <changeSet id="cat-outil-impact" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">SELECT COUNT(*) FROM category WHERE name = 'Outils d''impact'</sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT "Outils d'impact", "Outils d'impact", category_id
            FROM (SELECT category_id FROM category WHERE name = 'Outillage manuel') AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Outil pour la peinture ===== -->
    <changeSet id="cat-outil-peinture" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Outil pour la peinture'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Outil pour la peinture', 'Outil pour la peinture', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Outillage manuel') AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Outillage spécifique mécanique ===== -->
    <changeSet id="cat-outillage-mecanique" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Outillage spécifique mécanique'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Outillage spécifique mécanique', 'Outillage spécifique mécanique', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Outillage manuel') AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Autres outillages manuels ===== -->
    <changeSet id="cat-autres-outillages" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Autres outillages manuels'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Autres outillages manuels', 'Autres outillages manuels', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Outillage manuel') AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Electroportatifs ===== -->
    <changeSet id="cat-electroportatifs" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Electroportatifs'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Electroportatifs', 'Electroportatifs', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Quincaillerie' AND parent_id IS NULL) AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Droguerie ===== -->
    <changeSet id="cat-droguerie" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Droguerie'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Droguerie', 'Droguerie', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Quincaillerie' AND parent_id IS NULL) AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Accessoires ===== -->
    <changeSet id="cat-accessoires" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Accessoires'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Accessoires', 'Accessoires', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Quincaillerie' AND parent_id IS NULL) AS tmp;
        </sql>
    </changeSet>

    <!-- ===== MAISON (root) ===== -->
    <changeSet id="cat-maison" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Maison' AND parent_id IS NULL
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            VALUES ('Maison', 'Maison', NULL);
        </sql>
    </changeSet>

    <!-- ===== Jardin ===== -->
    <changeSet id="cat-jardin" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Jardin'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Jardin', 'Jardin', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Maison' AND parent_id IS NULL) AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Electricité et luminaires ===== -->
    <changeSet id="cat-electricite" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Electricité et luminaires'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Electricité et luminaires', 'Electricité et luminaires', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Maison' AND parent_id IS NULL) AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Sécurité ===== -->
    <changeSet id="cat-securite" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Sécurité'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Sécurité', 'Sécurité', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Maison' AND parent_id IS NULL) AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Sanitaire ===== -->
    <changeSet id="cat-sanitaire" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Sanitaire'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Sanitaire', 'Sanitaire', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Maison' AND parent_id IS NULL) AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Déco ===== -->
    <changeSet id="cat-deco" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Déco'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Déco', 'Déco', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Maison' AND parent_id IS NULL) AS tmp;
        </sql>
    </changeSet>

    <!-- ===== Cuisine ===== -->
    <changeSet id="cat-cuisine" author="dev">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM category WHERE name = 'Cuisine'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO category (name, description, parent_id)
            SELECT 'Cuisine', 'cuisine', category_id
            FROM (SELECT category_id FROM category WHERE name = 'Maison' AND parent_id IS NULL) AS tmp;
        </sql>
    </changeSet>

</databaseChangeLog>
